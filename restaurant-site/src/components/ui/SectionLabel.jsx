export default function SectionLabel({ children }) {
  return (
    <span className="inline-block tracking-[0.25em] text-xs uppercase font-sans"
      style={{ color: '#B8963E' }}>
      {children}
    </span>
  )
}
